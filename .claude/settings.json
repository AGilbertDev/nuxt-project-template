{
  "$comment": "Committed on purpose. A clone of this project gets the same workflow, so the plugins are enabled at project scope rather than per machine.",
  "extraKnownMarketplaces": {
    "agilbertdev": {
      "source": { "source": "github", "repo": "AGilbertDev/claude-plugins" }
    }
  },
  "enabledPlugins": {
    "workflow@agilbertdev": true,
    "nuxt-conventions@agilbertdev": true
  }
}
