export default defineAppConfig({
  // One primary and one neutral, themed through Nuxt UI. Pick the project's own
  // primary here rather than hardcoding a colour anywhere else.
  ui: {
    colors: { primary: 'teal', neutral: 'slate' }
  }
})
